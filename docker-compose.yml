version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: euroleague-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=euroleague_talk

volumes:
  mongo-data:
